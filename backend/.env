# .env for docker-compose
# Use the docker service name `db` and the PyMySQL dialect so the backend
# can connect to the MySQL container when run with docker-compose.
DATABASE_URL=mysql+pymysql://appuser:apppassword@db:3306/bookdb
